{
  "filters": [
    {
      "id": "brand_mentions",
      "chats_rules": [5000042366],
      "match": {
        "keywords_any": ["any","keyword"],
        "keywords_all": ["all", "keywords", "at", "once"],
        "regex": ["(?i)some\\s?regex", "(?i)another\\sregex"],
        "exclude_any": ["white", "black"],
        "exclude_regex": ["\\s(papper|coffie)\\s"]
      },
      "notify": {
        "urgent": true,
        "recipients": ["admin_main", "user_alice"],
        "forward": true,
        "template": "🔔 Brand mention\nKeywords: {{keywords}}\n🔗 {{message_link}}"
      }
    },
    {
      "id": "team_updates",
      "chats_rules": [5000042366, 5000042367],
      "match": {
        "keywords_any": ["update", "release"]
      },
      "notify": {
        "urgent": false,
        "recipients": ["chat_team", "channel_announcements"],
        "forward": false,
        "template": "📢 Team update: {{keywords}}"
      }
    }
  ]
}
